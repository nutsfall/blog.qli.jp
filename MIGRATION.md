# Markdown正規化プロジェクト

目的: 全記事（1874件）を Hugo に適したシンプルな Markdown に統一する。
過去のプラットフォーム移行（旧ブログ → Medium export → Medium RSS自動インポート）の変換痕跡を除去する。

## 運用ルール

- 修正はフェーズ単位でサブエージェント（Sonnet）が実施する
- 件数が多い課題は、ファイルを直接編集せず **スクリプトを書いて一括適用 → サンプル検証** する
- 修正前後で `scripts/content_lint.zsh` を実行し、対象課題の件数が減り、他の課題の件数が変わっていないことを確認する
- フェーズ完了ごとに `hugo build` で確認し、レビュー後にコミットする
- 作業中に新しい課題を見つけた場合、サブエージェントはこのファイルを直接編集せず、最終報告で親セッションに返す。親が「未分類の発見」に追記する

## フェーズ

### Phase 1: インポートパイプラインの計測ピクセル除去（完了）

- [x] 既存記事1件から `![](https://medium.com/_/stat?...)` を除去
- [x] インポート処理側で除去するようにし、今後の流入を止める
  - GitHub Actions 側（medium-import.yml 内の変換処理）は 2026-03-31 のコミット 96afbd6 で対応済みだった。今回 process_new_posts.rb にも除去処理を追加し二重の保険とした

### Phase 2: フロントマター正規化（完了）

- [x] 空の `categories: []` / `keywords: []` を削除（各1832件）
- [x] 空の `description` を削除（1534件。`''` 1386 + `""` 148）
- [x] 冗長な `draft: false` を削除（42件）
- [x] YAMLクォートスタイル統一（date 1658 / title 115 / description 5 をダブルクォート化）
- スクリプト: `scripts/frontmatter_normalizer.rb`（本文バイト不変・YAML意味等価をアサート済み）
- 新旧スキーマの `source:` / `original_url:` の差は残置（旧記事の元URLは復元不能。「保留 / 要決定」参照）

### Phase 3: リンク形式の正規化（完了）

- [x] Mediumリンクカードの崩れ `[**タイトル** _抜粋_](url "url")[](url)` → 通常リンクへ（315箇所 / 199ファイル）
- [x] 旧ブログの `リンク: [...](url "title").` 形式 → 通常リンクへ（189箇所）
- スクリプト: `scripts/link_normalizer.rb`
- スキップ3件（別種の壊れ方、手動対応が必要）:
  - `content/posts/200606/4b4fe3ab…` — ブラケットがエスケープされた `リンク: \[Title\](url)`
  - `content/posts/200607/55bdef0a…`, `content/posts/200609/f031e634…` — `リンク: <a title="` という壊れたHTML断片

### Phase 4: 画像（完了）

- [x] 空画像 `![]()` の処置（22ファイル23箇所。全件で画像が完全消失していたため行を削除。復元可能なものはなし）
- [x] 参照切れ画像の処置（200912/2502454f… の5参照を削除。実体は元からダミーGIF/Cloudflare認証ページで画像ではなかった。2025-09の「Fix ghost images」コミット 7ba4689a が参照を `.gif.gif` 等の二重拡張子に書き換えるバグを持っていた）
- [x] 未参照の孤児ファイル21件を削除（全件、同一バンドル内の参照済み画像とMD5完全一致の重複と確認済み。ユーザー承認の上で削除）
- 冒頭インライン画像の cover 化は Phase 7 に移動

### Phase 5: 定型ブロック（完了）

- [x] Amazonアフィリエイトリンク197箇所を `https://www.amazon.co.jp/dp/ASIN` に正規化（スキップ1件: 201508/a87ddf56… の `/pv` はASINなしの一般リンクのため残置）
- [x] Socialtunes の商品紹介ブロック88件を商品名リンクのみ残して削除
- スクリプト: `scripts/product_block_cleaner.rb`
- content_lint.zsh の「Amazonリンク」チェックを「未正規化のAmazonリンク」に変更

### Phase 6: 細かい残骸

- [ ] エスケープ残骸 `\_` `\*`（108件）
- [ ] 三点リーダ代用 `･･･`（64件）
- [ ] 見出しレベルの整理（`####` 681箇所が最多。ヒエラルキー不統一）
- [ ] draft記事2件の処置（`draft_-----------90caff9be54d/`、202405/eb8bae20…）

### Phase 7: 冒頭画像の cover 化（方針決定済み 2026-07-10: やる）

- [ ] 本文冒頭のインライン画像を `cover:` フロントマターへ移動（キャプション行があれば `caption:` に）。旧記事の見た目を新規インポート分と統一する

### Phase 8: description の重複削除（方針決定済み 2026-07-10: やる）

- [ ] 本文冒頭とほぼ完全重複している description を削除（299件中の該当分）。独自の要約になっているものは残す

## 保留 / 要決定

- UUID slug の扱い（変更すると既存URLが壊れるため現状維持が妥当か）
- 旧スキーマ1832件の `original_url` 補完（元URL情報が失われている）

## 未分類の発見

（作業中に見つかった新しい課題をここに追記する）

- Phase 2 で発見: 値ありの `description`（299件）の大半が本文冒頭の文とほぼ完全一致している（Mediumのsubtitle由来）。重複を削除するか、要約として活かすか要検討
- Phase 3 で発見: `[](http://click.linksynergy.com/...)` という空のアフィリエイト計測リンクが2ファイルに残存（`content/posts/200707/bae965d8…`, `content/posts/200709/b5e25879…`）
- Phase 3 で発見: 一部の旧記事タイトルに文字化け（`�`）が残っている（旧エンコーディング問題。件数未調査）
- Phase 5 で発見: `**発売日:**` のような太字形式の別系統の商品紹介テンプレートが5ファイルに存在（`content/posts/200609/0fa78392…` 等。Amazonリンク自体は正規化済み）
